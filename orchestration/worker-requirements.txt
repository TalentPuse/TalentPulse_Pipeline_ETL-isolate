# VNW worker: base deps + the skill-extraction stack.
# pipeline-skill-extraction.yml runs orchestration.flows.skill_extraction_pipeline
# out of THIS image, and src/extractors/skill_extractor.py imports openai + yaml.
# Neither is in base-requirements.txt, so the flow died on import before this.
-r base-requirements.txt
openai>=1.12.0
pyyaml>=6.0

# backup_pipeline: src/backup/r2_backup.py exports the dbt gold/feature models to
# zstd Parquet via pandas + pyarrow.
pandas>=2.2.0
pyarrow>=15.0.0
